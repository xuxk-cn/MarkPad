# MarkPad：为 Windows 打造的轻量 Markdown 写作桌

> 如果你厌倦了 Electron 笔记软件动辄数百兆的内存占用，又希望保留 Typora 那种「写着写着就变成排版好的文章」的体验，MarkPad 想在这条路上做一次认真的尝试——**一体化编辑、Rust 管文档、Tauri 做壳，前端尽量薄**。

---

## 一、它是什么？

**MarkPad** 是一款面向 **Windows 10/11** 的桌面 Markdown 编辑器（当前版本 0.1.0）。你可以把它理解成：

- **写作形态**：像 Typora 一样，**没有左右分栏**，只有一块编辑区；底层是 Markdown 源码，上面叠一层只读预览，标题、列表、粗体等会实时「长」在文字上方。
- **工程形态**：**Tauri 2** 提供窗口与文件对话框；**Rust** 用 `ropey` 管理全文，用 `pulldown-cmark` 做块级解析；**TypeScript + 原生 DOM** 负责界面，没有 React/Vue 整页框架。
- **使用形态**：本地文件、自动保存、最近文件、查找、右键菜单、中英界面——日常写笔记、改 README、整理对话导出的 Markdown 都够用。

它不是 Obsidian 的替代品（没有插件、图谱、双链），也不是 VS Code 的替代品（没有多语言 LSP、Git 面板）。它的定位很克制：**专注写一篇 Markdown，快、轻、顺眼**。

---

## 二、为什么值得试一试？

### 1. 一体化编辑，而不是「左边写、右边看」

很多编辑器默认分屏：左边源码，右边预览。对长文来说，眼睛要在两列之间跳；对习惯「写完再看效果」的人来说，也浪费横向空间。

MarkPad 从立项起就写死了一条红线：**不分栏**。编辑区只有一层可输入的 `contenteditable` 源码面（文字透明），上面是绝对定位的预览叠加层。你始终在「同一列」里移动光标，预览跟着走；失焦后，全文以排版样式呈现，适合通读。

这种体验对写技术文档、会议记录、博客草稿特别自然——你脑子里想的是「这一段是二级标题」，而不是「我要不要切到预览标签页」。

### 2. Rust 里的 Rope 是「单一事实源」

前端编辑区会频繁变动，但**真正被保存、被搜索、被解析的文本**在 Rust 侧：一条 `ropey` Rope。每次编辑防抖约 120ms 后，全文同步进 Rope；保存、自动落盘、文档内查找都基于它。

好处很实在：

- 大段粘贴、长文滚动时，不必让浏览器 DOM 承担「全文真相」的压力；
- 搜索在 Rust 里做 UTF-8 语义扫描，结果稳定，不依赖把整篇文档拆成巨型 DOM 树再遍历。

### 3. 轻量依赖，可预期的包体

项目刻意回避 Electron 和重型 UI 框架。`package.json` 里生产依赖只有 Tauri API 与对话框插件；渲染、菜单、设置都是手写 DOM。开发方案里甚至给 npm 依赖数量设了上限级别的约束——**性能与可维护性优先于功能堆叠**。

官方目标（讨论稿中的红线）包括：典型写作文档常驻内存 Win ≤ 60MB、安装包 ≤ 15MB 量级、输入响应 ≤ 8ms 等。即便你不在意 benchmark 数字，也能感受到设计方向：**这不是又一个「网页套壳」**。

### 4. 写作时的「小确幸」

这些能力叠加起来，日常很顺手：

| 能力 | 说明 |
|------|------|
| **Obsidian 式自动保存** | 文件一旦有过保存路径，停止输入约 1.2 秒写回磁盘，另有 30 秒兜底 |
| **最近文件** | 标题栏下拉，最多 10 条，快速回到昨天那篇笔记 |
| **新建 / 打开 / 保存** | 标题栏一键操作；`Ctrl+N` / `Ctrl+O` / `Ctrl+S` |
| **查找** | `Ctrl+F`，Rust 后端搜索，支持 F3 跳转 |
| **右键菜单** | 剪切 / 复制 / 粘贴；插入脚注、标注、分隔线、代码块、数学块；加粗、斜体、高亮等 |
| **智能粘贴** | 从网页、Word 复制时尝试读 HTML 并转成 Markdown，尽量保留标题与列表结构 |
| **GFM 表格** | 管道表在编辑时显示源码行，离开表格区域后以网格预览（类 Obsidian） |
| **表格 Tab** | 在表格内 Tab / Shift+Tab 在单元格间移动 |
| **设置** | 浅色 / 深色、中文 / English |
| **Ctrl + 滚轮** | 编辑区内调节字号 |
| **关联 .md 文件** | 安装后可在资源管理器中双击 `.md` 用 MarkPad 打开（需正确打包） |

---

## 三、界面长什么样？

打开应用后，结构很干净：

```
┌─────────────────────────────────────────────────────────┐
│ [Logo] 文件名 — MarkPad    [最近…][新建][打开][保存]…  │
├─────────────────────────────────────────────────────────┤
│ （可选）查找条：Ctrl+F 唤出                              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   这里是唯一的主编辑区                                   │
│   · 输入时：底层透明 Markdown 源码 + 上层彩色预览        │
│   · 选区时：显示源码文字，避免「双影」                   │
│   · 失焦时：全文以预览样式通读                         │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

没有侧边栏文件树、没有多标签、没有插件面板——**故意做减法**，把认知负担留给正文。

---

## 四、技术架构一瞥

对开发者读者，可以用一张简图理解数据流：

```
用户按键 / 粘贴
    ↓
#editor-source（contenteditable，plaintext-only）
    ↓ 防抖 ~120ms
invoke("text_set_all") → Rust Rope
    ↓
renderMarkdown（TS 行级）→ #editor-overlay（只读 HTML）
    ↓
保存 / 自动保存 / doc_search ← 均读 Rope
```

**前端（`src/`）** 主要模块：

- `editor.ts` — 双层编辑、叠加层同步、粘贴、IME、表格 Tab
- `renderer.ts` — 行级 Markdown → HTML（标题、列表、表格、行内样式）
- `paste-resolve.ts` — 剪贴板多 MIME 解析（plain / markdown / html）
- `context-menu.ts`、`markdown-edit.ts` — 右键与选区替换
- `main.ts` — 文件、查找、快捷键、自动保存
- `i18n.ts`、`settings.ts` — 语言与主题

**后端（`src-tauri/`）**：

- `ropey` 文档状态
- `file_io` — 打开 / 保存对话框、当前路径、读写磁盘
- `parser` — `pulldown-cmark` 块解析（供进阶渲染与测试）
- `search` — 文档内搜索命中
- `startup_open` — 启动参数与系统「打开文件」事件（含 macOS `Opened`）

**构建**：Vite 6 + TypeScript 5；`npm run tauri dev` 开发，`npm run tauri build` 打 Windows 安装包或便携 exe（见 `docs/打包说明.md`）。

测试方面，前端有 Vitest（渲染、粘贴解析、视口逻辑等），Rust 有 `cargo test` 与 `perf_10k` 基准，适合持续迭代时不踩回归。

---

## 五、和其他工具怎么选？

| 维度 | MarkPad | Typora | Obsidian | VS Code |
|------|---------|--------|----------|---------|
| 一体化 MD 写作 | ✅ 核心 | ✅ 成熟 | △ 需插件/模式 | △ 分栏或插件 |
| 本地优先 / 无账号 | ✅ | ✅ | ✅ | ✅ |
| 插件生态 | ❌ | ❌ | ✅ 极强 | ✅ 极强 |
| 双链 / 知识库 | ❌ | ❌ | ✅ | △ |
| 技术栈 | Tauri + Rust | 自研/Electron 历史 | Electron | Electron |
| 目标用户 | 要轻、要简单 | 付费专业写作 | 笔记网络 | 通用开发 |

**适合 MarkPad 的场景**：

- 写 README、设计文档、博客草稿；
- 从 Grok / 网页 / Word 粘贴内容并整理成 `.md`；
- 机器配置一般，不想长期挂一个重型 Electron 窗口；
- 愿意用「单文件 + 最近列表」管理笔记，而不是仓库式知识库。

**暂时不适合的场景**：

- 需要 Mermaid、复杂公式渲染、PDF 导出；
- 需要多标签、大纲侧栏、全局搜索；
- 需要插件或主题市场；
- 主要用 macOS / Linux（当前产品与文档以 **Windows** 为主，Tauri 可扩展但需自行验证）。

---

## 六、如何开始用？

### 从源码运行（开发者）

环境：**Node.js ≥ 18**、**Rust stable**、**WebView2**（Win11 通常已有）。

```powershell
git clone <你的仓库地址>
cd MarkPad
npm install
npm run tauri dev
```

首次 Rust 编译会下载较多 crate，请耐心等待。窗口出现后即可编辑；`npm run test:all` 可在提交前跑完全部检查。

### 终端用户

若已有打包好的安装程序或便携 `MarkPad.exe`，直接运行即可；目标机器需 **WebView2 Runtime**（Win11 一般自带）。更完整的打包路径见项目内 `docs/打包说明.md` 与 `使用手册.md`。

### 几个必会快捷键

| 快捷键 | 作用 |
|--------|------|
| Ctrl+N | 新建文档 |
| Ctrl+O | 打开文件 |
| Ctrl+S | 保存 |
| Ctrl+F | 查找 |
| F3 | 下一处结果 |
| Ctrl + 滚轮 | 调节编辑区字号 |
| Esc | 关闭查找条 |

---

## 七、诚实地说：还在进化中

MarkPad 是 **0.1.0** 阶段的个人/小团队向产品，有一些已知边界：

- **撤销/重做栈**、更完整的块级增量渲染、虚拟视口等仍在路线图里（见 `docs/开发方案讨论稿.md`）；
- 复杂 HTML 粘贴、嵌套列表、合并单元格表格等，无法保证与 Typora 完全一致；
- 长文档曾出现光标与预览行高错位问题，已通过「编辑时全文按源码行高渲染」等方式缓解，极端排版仍可能需反馈；
- 表格插入菜单曾暂缓，表格编辑以 GFM 管道语法 + Tab 导航为主。

我们更愿意把话说在前面：**它不是一个功能最全的 Markdown 平台，而是一个在性能与体验之间做了明确取舍的写作桌**。

---

## 八、结语

Markdown 编辑器很多，但真正让人愿意每天打开的，往往是「够轻、够快、别打断思路」。

MarkPad 的选择是：

1. **不分栏**的一体化写作；
2. **Rust Rope** 扛住文档与 I/O；
3. **薄前端** 扛住交互与渲染；
4. **功能做减法**，把右键菜单、自动保存、智能粘贴、表格与查找做到够用。

如果你也在 Windows 上寻找一款「打开就能写、保存就是 `.md`、不占太多内存」的工具，不妨 clone 下来跑一次 `npm run tauri dev`，或者等下一版安装包——用一篇长文、一次大粘贴、一次深夜自动保存，感受一下它是不是你的菜。

---

**相关链接（仓库内）**

- [README.md](../README.md) — 项目简介与脚本索引  
- [使用手册.md](../使用手册.md) — 面向最终用户的简明说明  
- [docs/开发方案讨论稿.md](./开发方案讨论稿.md) — 架构红线与路线图  
- [docs/打包说明.md](./打包说明.md) — Windows 安装包与便携版构建  

*本文基于 MarkPad 0.1.0 代码与文档整理，随版本迭代部分内容可能过时，以仓库最新说明为准。*
