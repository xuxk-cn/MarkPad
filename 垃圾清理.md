已按`.gitignore`清理构建产物，共释放约4.78 GB：

| 已删除 | 大小 |
| --- | --- |
| `src-tauri/target/` | 4779 MB |
| `dist/` | 0.6 MB |
| `output/` | — |

保留未删：

- `backup01/`（代码备份）
- `node_modules/`（依赖，删掉后需`npm install`）
- `可执行文件/`（本地发布目录）

需要恢复构建时：`npm run build`或`npm run tauri build`。若要一并删除`node_modules`或`test.html`，可以说一声。