# MarkPad 使用手册

> 面向**最终用户**的简明说明。安装、构建、排障与开发者命令见 **`docs/启动与使用手册.md`**；最短命令见 **`RUN.md`**。

---

## 1. 软件能做什么

**MarkPad** 是在 Windows 上使用的 **Markdown 编辑器**：

- 在同一编辑区里改源码，上面叠一层「预览样式」（标题、列表、加粗等），**没有**左右分栏预览。
- 文档内容在后台用 **Rust** 管理；适合日常写笔记、改文档、看格式效果。

---

## 2. 安装与打开

1. 电脑已安装 **Node.js**（建议 18 或以上）和 **Rust**（stable）。  
2. 拿到本程序源码后，在 **PowerShell** 里进入项目文件夹，执行：

```powershell
npm install
npm run tauri dev
```

第一次运行会编译较久，请耐心等待；结束后会出现 MarkPad 窗口。

**环境说明**：需要 **WebView2**（Windows 11 一般已有；Windows 10 若打不开窗口，请按微软说明安装 [WebView2 Runtime](https://developer.microsoft.com/microsoft-edge/webview2/)）。

---

## 3. 界面说明

| 区域 | 作用 |
|------|------|
| **标题栏左侧** | Logo + 当前文件名（已保存会带路径名；未保存为「未命名」） |
| **最近…** | 从最近用过的文件中选一个打开（最多保留 10 条） |
| **打开** | 用系统对话框选一个文件 |
| **保存** | 保存到当前文件；若是新文件，会弹出位置让你选 |
| **复制** | 把当前全文（Markdown 源码）复制到剪贴板 |
| **设置** | 主题、语言、当前字号说明 |

---

## 4. 编辑与保存

- 在正文区直接输入 **Markdown**；光标与选区在「源码层」，你看到的是底层文字透明、上面叠的彩色格式层。
- **已经打开或保存过到某个文件**时：停止输入约 **1.2 秒**后会**自动保存**到该文件（类似 Obsidian）；另有约 **30 秒**一次的兜底保存。
- **从未保存过的新文档**不会自动写到硬盘；请先点 **保存** 或按 **Ctrl+S** 选一个路径，之后才会对该文件自动保存。
- 任何时候都可以 **Ctrl+S** 立刻保存。

---

## 5. 设置（外观与语言）

点击 **设置**：

- **浅色 / 深色**：应用内切换，不强制跟随系统。
- **中文 / English**：切换界面语言（按钮、查找条、右键菜单等）。

---

## 6. 字号（Ctrl + 滚轮）

在**编辑区域**内，按住 **Ctrl**（Mac 键盘为 **Command**）并**滚动鼠标滚轮**，可以放大或缩小正文字号（有上下限）。设置窗口底部会显示当前字号。

---

## 7. 右键菜单

在编辑区**右键**可打开菜单：

**插入**：脚注、表格、标注（引用样式）、分隔线、代码块、数学块等。

**文本格式**：加粗、倾斜、删除线、高亮、行内代码、行内数学、HTML 注释、清除格式等。  
（有选中内容则包在选区外；没有选中会插入占位文字再改。）

---

## 8. 查找

| 操作 | 快捷键 |
|------|--------|
| 打开查找 | **Ctrl+F** |
| 关闭查找条 | **Esc** |
| 在查找框里搜索 | **Enter** |
| 下一处 | **F3** 或「下一个」按钮 |

---

## 9. 常用快捷键一览

| 快捷键 | 作用 |
|--------|------|
| **Ctrl+S** | 保存 |
| **Ctrl+O** | 打开文件 |
| **Ctrl+F** | 查找 |
| **F3** | 下一处查找结果 |
| **Esc** | 关闭查找条 |
| **Ctrl + 滚轮** | 在编辑区内调节字号 |

---

## 10. 安装包与免安装 exe

完整流程、环境要求与「何时需手动清理」见 **`docs/打包说明.md`**（**推荐先读**）。以下为摘要：

| 脚本 | 产物 |
|------|------|
| **`scripts/build-non-standalone.bat`** | Windows **安装程序**（NSIS + MSI），需安装后使用；产物在 **`output\release\bundle\`**（脚本将 `CARGO_TARGET_DIR` 指到项目根 `output`）。成功后 **仅保留 `bundle\` 内安装介质**（根目录未入包的 exe/dll 及 Cargo 中间目录、bundle 内 NSIS/WiX 源与中间文件会删掉）。 |
| **`scripts/build-portable-no-installer.bat`** | **免安装 exe**（`tauri build --no-bundle`），在 **`output\release\`** 下；成功后 **仅保留 `.exe` 与 `.dll`**（删除 `bundle`、deps/build 等）。仍依赖本机 **WebView2**。可执行文件为 **`MarkPad.exe`**。 |

仅复制 `.exe` 分发时，请说明目标机器需已安装 WebView2 Runtime。

---

## 11. 更多说明

| 文档 | 内容 |
|------|------|
| **`README.md`** | 项目简介（中英索引）、仓库结构、npm 脚本 |
| **`docs/打包说明.md`** | Windows 打包流程、产物路径、`CARGO_TARGET_DIR` 与清理脚本说明 |
| **`docs/开发方案讨论稿.md`** | 产品与技术方案 |
| **`RUN.md`** | 最短运行步骤与 cargo 镜像示例 |

如有功能建议或缺陷，可在仓库 Issue 中反馈（若已启用远程仓库）。
